class HelloWorld {
    public static void main(String[] args) {
        int a[]={2,4,7,1,5,3,8,6,7,1,10,13,12,11};///1,2,3,4,5,7;
    for(int k=0;k<a.length-1;k++) 
    {
        for(int i=0;i<a.length-1-k;i++)
        {
          if(a[i]>a[i+1])
          {
              int temp=a[i];
              a[i]=a[i+1];
              a[i+1]=temp;
          }
        }
    }
        for(int i=0;i<a.length;i++)
          System.out.println(a[i]);
    }
}
